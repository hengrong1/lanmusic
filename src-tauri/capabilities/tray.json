{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tray-menu",
  "description": "Capability for the tray menu popup window",
  "windows": ["tray"],
  "permissions": [
    "core:default",
    "core:window:allow-hide"
  ]
}
