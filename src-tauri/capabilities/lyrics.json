{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "lyrics-window",
  "description": "Capability for the desktop lyrics window",
  "windows": ["lyrics"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-close"
  ]
}
